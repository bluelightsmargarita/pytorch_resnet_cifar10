model:
  arch: resnet32

dataset:
  name: cifar10

optimizer:
  type: SGD
  lr: 0.1
  momentum: 0.9
  weight_decay: 0.0001

scheduler:
  type: MultiStepLR
  milestones:
    - 100
    - 150
  gamma: 0.1

training:
  epochs: 200
  batch_size: 128